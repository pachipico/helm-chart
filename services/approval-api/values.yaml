name: approval-api

service:
  name: approval-api
  type: ClusterIP
  port: 9094

image:
  repository: miroit/approval-api
tag: '5b7d5b7'

replicaCount: 1

global:
  logPath: /root/logs

spring:
  profiles:
    active: dev