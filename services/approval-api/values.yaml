name: approval-api

service:
  name: approval-api
  type: ClusterIP
  port: 9094

image:
  repository: miroit/approval-api
tag: '16b63b5'

replicaCount: 1

global:
  logPath: /root/logs

spring:
  profiles:
    active: dev