name: config-server

service:
  name: config-server
  type: ClusterIP
  port: 8888

image:
  repository: miroit/config-server
tag: '0359a42'

replicaCount: 1

global:
  logPath: /root/logs

env:
  - name: GIT_USERNAME
    valueFrom:
      secretKeyRef:
        name: github-auth
        key: username
  - name: GIT_PASSWORD
    valueFrom:
      secretKeyRef:
        name: github-auth
        key: password