name: hr-api

service:
  name: hr-api
  type: ClusterIP
  port: 9095

image:
  repository: miroit/hr-api
tag: '068c020'

replicaCount: 1

global:
  logPath: /root/logs

spring:
  profiles:
    active: dev