apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.service.name }}-pv
  labels:
    type: local
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/root/nexus"