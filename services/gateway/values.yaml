name: gateway

service:
  name: gateway
  type: ClusterIP
  port: 9000

image:
  repository: miroit/gateway
tag: 'd8e0638'

replicaCount: 1

global:
  logPath: /root/logs

spring:
  profiles:
    active: dev