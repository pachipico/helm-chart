name: gateway

service:
  name: gateway
  type: ClusterIP
  port: 9000

image:
  repository: miroit/gateway
tag: '9be2e9e'

replicaCount: 1

global:
  logPath: /root/logs

spring:
  profiles:
    active: dev