name: gateway

service:
  name: gateway
  type: ClusterIP
  port: 9000

image:
  repository: miroit/gateway
tag: '5b1b73b'

replicaCount: 1

global:
  logPath: /root/logs

spring:
  profiles:
    active: dev